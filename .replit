
modules = ["web"]
run = "npx http-server -p 5000 --bind 0.0.0.0"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npx http-server -p 5000 --bind 0.0.0.0"]

[workflows]
runButton = "Serve Website"

[[workflows.workflow]]
name = "Serve Website"
author = 41234181
mode = "sequential"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "npx http-server -p 5000 --bind 0.0.0.0"
